apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: single-minio-pv-claim2
spec:
  # storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
